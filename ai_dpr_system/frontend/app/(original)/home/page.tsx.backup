// Disabled legacy route kept for reference only.
// This file is ignored by Next.js because of the .backup extension.
// If you need to re-enable it as a route, rename to page.tsx and ensure it does not break builds.

export default function OriginalHomePage() {
	return (
		<div className="p-6">
			<h1 className="text-2xl font-bold">Original Home</h1>
			<p className="text-muted-foreground mt-2">This legacy route is kept for reference.</p>
		</div>
	);
}
